# Sanity CMS — https://sanity.io → твой проект → Settings → API
PUBLIC_SANITY_PROJECT_ID=your_project_id_here
PUBLIC_SANITY_DATASET=production

# Sanity API Token — Settings → API → Tokens → Add API token (Editor)
# ⚠️ Никогда не публикуй этот токен в git!
SANITY_API_TOKEN=your_api_token_here
